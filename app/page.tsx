import RedirectToForcesPaginatedPage from "@/features/forces/utils";

// 💭 Redirecting to the SSG'd forces/1 page - a thought, if there was a CMS API data I could put prose in here with a link to the paginated forces listing page
const RootPage = RedirectToForcesPaginatedPage;

export default RootPage;
